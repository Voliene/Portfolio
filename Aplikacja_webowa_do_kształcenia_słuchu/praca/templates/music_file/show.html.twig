{% extends 'base.html.twig' %}

{% block title %}MusicFile{% endblock %}

{% block body %}
    <h1>MusicFile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ music_file.id }}</td>
            </tr>
            <tr>
                <th>FileName</th>
                <td>{{ music_file.fileName }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('app_music_file_index') }}">back to list</a>

    <a href="{{ path('app_music_file_edit', {'id': music_file.id}) }}">edit</a>

    {{ include('music_file/_delete_form.html.twig') }}
{% endblock %}
